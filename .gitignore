# R / RStudio
.Rhistory
.RData
.Rproj.user/
.Renviron

# OS / editor
.DS_Store
Thumbs.db

# Data folders
# - Keep raw + intermediate ignored (too large / reproducible from pipeline)
# - Commit final + validation so teammates can use them directly
data/raw/**
data/intermediate/**
!data/final/**
!data/validation/**

# Allow placeholder files
!data/**/.gitkeep

